// Show summary before request is sent
/* 
- Show summary: vehicle, route, payment (free for now)
- "Book via WhatsApp" (link to Ace) or mock button
*/

import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const RideConfirmation = () => {
  return (
    <View>
      <Text>RideConfirmation</Text>
    </View>
  )
}

export default RideConfirmation

const styles = StyleSheet.create({})